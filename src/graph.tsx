import React from "react";
import {
  BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend,
} from 'recharts';

const Graph = () => {

  return (
    <section>

    </section>
  );
};

export default Graph;
